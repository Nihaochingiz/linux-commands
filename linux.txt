ls -l.
Показать ограничения прав
ls -a
Вывести все файлы, включая скрытые
cd /home
переход в домашнюю директорию
cat .bash_logout
прочитать файл .bash_logout
head
посмотреть начало файла 10 строк
tail
посмотреть конец файла 10 строк
tail -n 2 .bashrc
посмотреть последние 2 строки, через параметр -n
tail -f path/to/file
Команда   выводит последние строчки файла и ждет появления новых

Особая категория программ для просмотра содержимого файла — пейджеры. Пейджер похож на текстовый редактор, открытый только в режиме чтения. Самый распространенный пейджер называется less.

grep
поиск определенного текста
grep aliases .bashrc
поиск определенного текста по файлу или файлам.

vim solution Esc i Hello,World! Ctrl+[ :wq


cat source > /tmp/result
Прочитайте в командной строке содержимое файла source и перенаправьте его в файл /tmp/result, которого не существует