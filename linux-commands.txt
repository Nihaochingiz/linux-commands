$echo PWD
Показать текущую рабочую директорию

ls
Она выводит список файлов и 
директорий в текущей рабочей директории

cd /home

Перейти к директории

cd ../..

Также перейти к директории

ls /opt
Показать содержимое директории

ls ..

cat languages | sort | head -n 2

С помощью конвейера отсортируйте содержимое файла 
languages в алфавитном порядке
и выведите на экран только первые две строки

touch empty-file
Создать файл

rm empty-file

Удалить файл

touch file
mv file renamed-file
Создать файл и переименовать его

cp renamed-file renamed-file-copy

Копирование файла

mkdir my-dir
Создать директорию

mkdir -p one/two/three
Создание директорий рекурсивно

rm -r my-dir
Удаление директорий, можно делать только рекурсивно

rm -rf one
Удаление всей директории без вопросов

env
Посмотреть установленные переменные


echo $HOME

/home/kirill.m

export HOME=/tmp
cd
pwd

/tmp

echo $HOME

/tmp


export MAIL=/var/tmp NEW_VAR=value

ctrl + r
mail
искать команду со словом mail

whoami
выяснить имя пользователя

ps
отчет о работающих процессах

ps aux
Информацию о том, какой процесс и под каким пользователем запущен

id

Посмотреть свой идентификатор

id

Группы, ассоциированные с текущим пользователем, показываются в выводе команды  

Крайне не рекомендуем использовать root на регулярной основе, 
и ни в коем случае не входить под ним в систему. Суперпользователь — это прямой доступ ко всему и большая дыра в безопасности системы.


id nobody -u
Вывести только id пользователя

 sudo 
 Основной способ повышать привилегии в современных системах 
 
 sudo touch /etc/myfile
повысить роли к файлу myfile

sudo -u nobody mkdir /tmp/test
выполнить команду из-под пользователя, отличного от root

echo 'Hello, World!' | sudo tee config/myfile



Кроме имени пользователя и группы, с каждым файлом ассоциированы права доступа:

r — чтение (read)
w — запись (write)
x — исполнение (execute)

chmod 511 myfile
Для владельца файла установите разрешение только на чтение и исполнение файла. Для пользователей, входящих в группу владельца, и для остальных пользователей установите разрешение только на исполнение

chmod u+x program
дать права на исполнение
./program
запуск файла